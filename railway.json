{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "PORT=4173 BASE_PATH=/ PUBLIC_SITE_URL=https://jack.torchlabs.ca VITE_DISABLE_CLERK_PROXY=true pnpm --filter ./artifacts/jack-core build && pnpm --filter ./artifacts/api-server build"
  },
  "deploy": {
    "startCommand": "pnpm --filter ./artifacts/api-server start",
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
